
using AutoMapper;
using BlazorAppForClient.ViewModels;
using System.Net;

namespace BlazorAppForClient.Extensions
{
  /*  public class MapperProfile : Profile
    {


        public MapperProfile()
        {
            CreateJobMap();
         
        }



        private void CreateJobMap()
        {
            CreateMap<CompleteJodWithDetailsViewModel, CompleteJobViewModel>()
                .ForMember(r => r.id, opt => opt.MapFrom(p => p.jodDetails.id))
                .ForMember(r => r.model, opt => opt.MapFrom(p => p.jodDetails.m))
                .ForMember(r => r.id, opt => opt.MapFrom(p => p.jodDetails.id))
                .ForMember(r => r.id, opt => opt.MapFrom(p => p.jodDetails.id))
                .ForMember(r => r.id, opt => opt.MapFrom(p => p.jodDetails.id))
                .ForMember(r => r.id, opt => opt.MapFrom(p => p.jodDetails.id))

                .ForMember(r => r.status, opt => opt.MapFrom(p => p.status))
                .ForMember(r => r., opt => opt.MapFrom(mech => $"{mech.Mechanic.FirstName} {mech.Mechanic.LastName}"))
                .ForMember(r => r.Model, opt => opt.MapFrom(model => model.Model.Name)).ReverseMap();
                ;
   
        }





    }*/
}
